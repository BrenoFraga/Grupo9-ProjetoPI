package findr.projectfindr.repository;

import findr.projectfindr.model.MockFreelancer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MockFreelancerRepository extends JpaRepository<MockFreelancer,Long> {
}
